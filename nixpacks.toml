# 공유 썸네일에 한글을 그리려면 서버에 한글 글꼴이 있어야 한다.
#
# 썸네일은 지도 이미지 위에 SVG를 얹어 만드는데, 그 SVG 안에 가게 이름과 메모가
# 한글로 들어간다. 기본 빌드 이미지에는 CJK 글꼴이 없어서 이 줄이 없으면 글자가
# 통째로 빈칸이나 두부 모양으로 나온다. 핀과 선은 멀쩡해 보이므로 알아채기 어렵다.
[phases.setup]
aptPkgs = ["fonts-noto-cjk"]
