/**
 * `server-only`는 클라이언트 컴포넌트에서 import되면 빌드를 실패시키는 표식 패키지다.
 * 그 판단은 번들러가 하는데, vitest는 번들러가 아니라 무조건 실패한다.
 *
 * 테스트에서는 빈 모듈로 대체한다. 실제 빌드에서는 원본이 그대로 쓰이므로
 * 서버 전용 보호는 유지된다. 별칭은 vitest.config.ts에 있다.
 */
export {};
