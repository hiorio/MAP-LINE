# MAP-LINE — 작업 지시서

손으로 그리는 지도/동선 공유 서비스. **iOS 앱이 기본이고 웹은 링크를 받은 사람이
아무것도 설치하지 않고 여는 자리**입니다. 안드로이드는 한참 뒤입니다.

## 먼저 읽을 것

1. **[HANDOFF.md](HANDOFF.md)** — 지금 뭐가 되고, 뭐가 안 되고, 다음에 뭘 할지.
   **작업 시작 전 반드시 읽으세요.**
2. **[ios/README.md](ios/README.md)** — 앱을 건드린다면. 구조, 빌드 방식,
   SDK 시그니처 확인법, 이미 밟은 함정 전부.
3. [README.md](README.md) — "왜 이렇게 했는가"가 궁금할 때.
4. [docs/ROADMAP.md](docs/ROADMAP.md) — 장기 계획. 일부는 이미 다른 결론이 났고
   그 자리에 표시돼 있습니다.

## 이 저장소에서 특히 조심할 것

### 맥이 없습니다

개발 환경이 윈도우입니다. **GitHub Actions의 macOS 러너가 앱의 유일한 컴파일러이고
한 번에 약 10분입니다.** 추측으로 고쳐 밀어 넣지 마세요.

- SDK 시그니처는 바이너리에서 직접 읽을 수 있습니다 → `ios/README.md`의
  "SDK 시그니처를 확인하는 법". 추측이 제일 비쌉니다.
- **눈으로 볼 방법은 UI 테스트 스크린샷뿐입니다.** 새 화면·새 그림을 만들면 그 흐름을
  걸어가는 UI 테스트를 함께 넣으세요. 안 넣으면 어떻게 보이는지 아무도 모릅니다.
- 그림만으로 원인을 못 좁히면 숫자를 `view.accessibilityValue`로 내보내 UI 테스트가
  받아 가게 하세요. 디버거를 붙일 수 없는 환경에서 이게 디버거 대신입니다.

```bash
gh run list --workflow=ios.yml --limit 1
gh run view <id> --json status,conclusion
gh run download <id> -n ui-screenshots -D shots   # screenshots/manifest.json에 이름↔파일 대응
```

### 웹과 앱이 같은 답을 내야 합니다

서버 API(`app/api/**`)는 한 벌이고 웹·앱이 같이 씁니다. 앱은 카카오 REST 키를 갖지
않습니다(뜯기면 남이 우리 쿼터를 태웁니다).

같은 규칙이 양쪽에 각각 있습니다. **한쪽만 고치면 같은 지도가 두 곳에서 다르게
그려지고, 그건 링크를 열어 보기 전까지 아무도 모릅니다.** 짝 목록은 HANDOFF.md의
"웹과 앱이 나눠 갖는 것" 표에 있습니다.

앱에서 뭔가 이상해 보이면 **웹의 같은 부분을 먼저 읽으세요.** 웹이 일부러 그렇게 해
둔 것을 버그로 오해해 두 번 헛고친 적이 있습니다.

### 실기기 확인이 하나도 안 됐습니다

코드와 CI는 초록이지만 손가락으로는 한 번도 안 돌려봤습니다. 앱은 CI 시뮬레이터
스크린샷으로만, 웹은 데스크톱 브라우저로만 봤습니다. **완료로 취급하지 마세요.**

### TestFlight는 수동입니다

push하면 `ios.yml`(빌드·테스트·스크린샷)만 자동으로 돕니다. TestFlight 업로드는
`testflight.yml`을 수동 실행(`gh workflow run testflight.yml --ref main`)해야 합니다.
빌드 번호는 `github.run_number`로 자동 부여되므로 충돌하지 않습니다.

**아이콘·시각물은 올리기 전에 사용자에게 보여 주고 확인받으세요.**

## 실행

```bash
# 웹
npm run dev            # http://localhost:3000
npm test               # 도메인·통합 테스트
npm run typecheck && npm run lint

# 앱 — 로컬에서 못 돌립니다. push하고 CI를 봅니다.
```

`npm run dev`가 떠 있을 때 `npm run build`를 돌리면 `.next`가 깨집니다.

## 문서를 고칠 때

작업이 끝나면 **HANDOFF.md를 같이 갱신하세요.** 이 저장소는 세션이 자주 바뀌고,
문서가 뒤처지면 다음 세션이 이미 끝난 일을 다시 하거나 없는 기능을 있다고 믿습니다.
계획이 바뀌었으면 원래 계획을 지우지 말고 그 자리에 왜 바뀌었는지 남기세요.
