# HANDOFF — MAP-LINE

마지막 갱신: 2026-08-08

다른 AI 세션이 이 문서 하나만 읽고 바로 이어서 작업할 수 있도록 쓴 인수인계 문서입니다.
설계 배경과 근거는 [README.md](README.md)에 있으니 "왜 이렇게 했는가"가 궁금하면 거기를 봅니다.
이 문서는 "지금 뭐가 되고, 뭐가 안 되고, 다음에 뭘 하면 되는가"만 다룹니다.

## 지금 상태를 한 줄로

**웹은 `cc027a5`까지 운영 배포됐고 iOS 1.0 빌드 33은 TestFlight 및 App Store `WAITING_FOR_REVIEW` 상태입니다. 중간지점 자동차 참가자, 지도 상단 검색바·자동차 동선·선택 단계 새 동선 분리 등 후속 변경은 소스에만 있으며 아직 웹 운영·새 앱 빌드에는 포함되지 않았습니다.**

### 2026-08-08 로컬 저장소 경로 이전

- Windows의 기본 작업 경로를 `C:\MAP-LINE`에서 `C:\Projects\MAP-LINE`으로 옮겼다.
  이후 로컬 명령과 새 세션은 새 경로를 기준으로 사용한다.
- `.tmp/`는 CI 다운로드·Xcode 결과 번들·스크린샷 변환본처럼 다시 만들 수 있는 대용량
  작업 산출물이므로 Git에서 제외한다. 최종 App Store 증빙 스크린샷은 `appstore/`에 보존한다.

### 2026-08-08 선택한 단계로 새 동선 만들기 — 소스만 반영, 앱 빌드 안 함

- 동선 시트 왼쪽에 `선택`을 추가했다. 선택 모드에서는 단계별 후보를 편집하는 복잡한 행 대신
  체크 원·단계 번호·장소명을 한 행 전체 터치 영역으로 보여 주며 여러 단계를 고를 수 있다.
  하단 `선택한 단계로 새 동선 만들기`에서 이름을 확인한 뒤 원본을 그대로 둔 복제본을 만든다.
- 동선 시트를 중간/큰 높이로 열고 선택된 단계 핀만 원래 색으로 남기며 나머지는 회색으로
  낮춘다. iOS 16.4 이상에서는 선택 모드·중간 높이일 때 뒤의 지도 단계 핀을 직접 눌러도 목록의
  같은 단계가 선택·해제된다. 그 외 지도 POI·보관함·메모·롱프레스는 선택 중 오작동하지 않는다.
- `extractedRouteDocument`는 원본 순서로 선택 단계를 거르고 그 후보와 장소 자체 메모를 보존한다.
  원래도 인접한 단계 쌍만 이동수단·경로를 유지하고, 중간 단계를 건너뛰어 새로 맞닿은 쌍은
  직선으로 초기화한다. 지도 전체 손그림과 자유 메모는 단계 소유 관계가 없어 복제하지 않는다.
- 새 문서는 선택 장소 평균점을 중심으로 서버에 별도 생성·저장하고, 성공하면 시트를 닫아 새
  동선을 바로 연 뒤 기존 전체 콘텐츠 맞춤 로직으로 카메라를 이동한다. 저장 실패 시 원본과 선택
  상태를 유지한 채 오류를 표시한다.
- 선택 추출 순서·인접 경로 보존·비인접 경로 초기화 단위 테스트와 UI 선택 식별자 검증을
  추가했다. Windows에서는 Swift/iOS 빌드를 실행할 수 없어 macOS CI 검증은 아직 필요하다.

### 2026-08-08 중간지점 자동차 참가자 — 소스만 반영, 배포·앱 빌드 안 함

- 일반 동선은 이미 자동차를 지원했지만 `/api/midpoint` 입력 검증과 iOS 참가자 모드가
  도보·대중교통·자전거로 제한돼 있던 불일치를 해소했다. 중간지점 참가자마다 자동차를
  다른 이동수단과 섞어 선택할 수 있고, 최종 후보 순위에는 Kakao Mobility의 실제 교통
  경로 시간·거리·좌표가 반영된다.
- 실제 경로를 부르기 전 후보 압축에도 자동차를 추가했다. 도심 약 32km/h와 접근 5분을
  근삿값으로 쓰고 중심 가중치는 0.75로 두어 빠른 자동차 출발지 쪽으로 후보 중심을 과하게
  끌지 않는다. 이 값은 비용 절감용 사전 압축일 뿐 최종 순위는 실제 API 시간이 결정한다.
- iOS의 참가자 이동수단 선택은 네 항목 segmented picker 대신 가로 스크롤 칩으로 바꿔
  작은 화면에서도 `대중교통`과 `자동차`가 잘리지 않는다. 서버가 반환한 car 경로는 기존
  `TravelMode.car`와 주황색 경로 스타일을 그대로 사용해 후보 지도에 표시된다.
- 자동차 길찾기 좌표·거리·시간은 현재 결과 화면에서만 유지한다. 중간지점 기록에는 참가자
  위치·`car` 선택·후보 순서만 남기고 자동차 leg 및 그 값으로 만든 후보 시간 요약을 제거한다.
  기록을 다시 열면 `다시 계산 필요`와 현재 교통량으로 재검색하라는 안내를 표시한다. 도보·
  대중교통·자전거 경로 기록은 기존처럼 보존한다.
- 새 API 입력 검증과 자동차 가중치 테스트를 포함해 DB 통합을 제외한 웹 테스트 303개,
  TypeScript 검사와 운영 빌드가 통과했다. ESLint 오류는 없고 기존 nearby 테스트 경고 1건만
  남는다. Swift/iOS 테스트는 Windows에서 실행할 수 없어 UI 식별자·기록 단위 테스트만 추가했다.
- 이번 작업에서는 실제 자동차 API를 추가 호출하지 않았고 커밋·푸시·웹 재배포·iOS CI·
  TestFlight를 실행하지 않았다. 서버 입력 변경도 운영에 적용하려면 웹 배포가 한 번 필요하다.

### 2026-08-07 카카오 그룹 안내·자동차 동선 웹 운영 배포 — iOS 빌드 안 함

- 실제 공유 원문 `https://kko.to/DzukR3dcsd0`을 서버와 모바일 UA로 열어도 404였고,
  Kakao의 공개 API에는 저장 장소 그룹의 내부 목록을 받는 기능이 없다. iOS 공유 확장에는
  `[카카오맵] 음식점 그룹`과 링크만 넘어오므로 그룹 안 장소를 안정적으로 가져올 수 없다.
- 이 입력을 `음식점 그룹`이라는 일반 장소명으로 재검색해 프린지그룹·스몰그룹 같은 엉뚱한
  후보를 만들던 동작을 막았다. 서버가 그룹형 공유를 422로 구분해 개별 장소 공유 또는
  장소명·주소 목록을 요청하고, iOS는 서버의 구체적인 안내 문구를 그대로 표시한다.
- 지도 오른쪽의 중복 `장소 추가` 원형 버튼을 없애고, 상단에 `장소나 주소 검색` 바를 두어
  기존 복수 검색·새 단계/기존 단계 후보 지정 화면을 바로 연다. UI 테스트 식별자와 크기
  검증도 검색바 기준으로 갱신했다.
- 일반 동선 이동수단에 `자동차`를 추가했다. 서버는 Kakao Mobility Directions
  `/v1/directions`의 도로별 평면 vertex 배열을 경로 좌표로 변환하고, 앱·웹은 주황색 실선과
  `자동차 · 거리 · 시간` 라벨로 표시한다. iOS의 다섯 수단은 작은 화면에서 잘리지 않도록
  segmented picker 대신 가로 스크롤 칩으로 바꿨다.
- 과거 Kakao Mobility 공식 답변에 따라 자동차 경로 결과는 자체 DB에 저장하지 않는다.
  `mode: car`만 사용자 데이터로 저장하고 좌표·거리·시간은 편집 세션에서만 유지한다. iOS
  기기 초안, 웹 브라우저 초안, 웹 저장 API, DB 트리거 네 층에서 자동차 route를 제거하며,
  공유 웹은 사용자가 `모임 동선`을 열 때만 실시간 조회한다. 자동차 경로는 1시간이 지나면
  같은 편집 세션에서도 교통량 갱신 대상으로 본다.
- DB에는 `stop_legs.mode = car`만 허용하고 캐시 컬럼을 강제로 비우는
  `0011_stop_legs_car.sql`을 추가했고 운영 Supabase에 적용했다. 원격 마이그레이션 목록에서
  `0011` 반영을 재확인했으며, 실제 DB 왕복 테스트에서도 자동차 모드는 남고 route는 제거됐다.
- 운영 REST 키로 Kakao Mobility를 직접 한 번 호출해 `result_code=0`, 10.345km, 1,383초,
  좌표 150개를 확인했다. 배포 뒤 운영 `/api/route?mode=car`도 200, 11.721km, 1,416초,
  좌표 185개를 반환해 Railway 환경변수와 서버 정규화까지 끝단 검증했다.
- 전체 웹 테스트 334개(운영 Supabase 통합 36개 포함), TypeScript 검사와 운영 빌드가 통과했다.
  ESLint 오류는 없고 기존 nearby 테스트의 미사용 인자 경고 1건만 남는다.
- 웹·DB 파일만 선별한 `cc027a5`를 작업 브랜치와 `main`에 푸시했고 Railway 상태가 `success`다.
  운영 그룹 공유 API는 문제 입력에 422와 개별 공유/목록 붙여넣기 안내를 반환한다.
- Swift 빌드·iOS 테스트·TestFlight는 실행하지 않았다. 따라서 앱 상단 검색바와 앱의 자동차
  선택 UI는 다음 iOS 빌드 전까지 TestFlight 33에서 보이지 않지만, 새 운영 서버 API는 이미
  사용할 수 있다.

### 2026-08-07 보관함·공유 폴더·현재 위치 재포커스 개선 — 소스만 반영, 빌드 안 함

- 지도 위 개인 보관 마커를 22pt에서 19pt로 3pt 더 줄이고, 현재 위치 파란 마커도
  28pt에서 23pt로 5pt 줄였다. 카카오 SDK가 같은 스타일 ID의 이전 이미지를 재사용하지
  않도록 두 스타일 ID 버전도 함께 올렸다.
- 보관함 폴더 마크를 8종에서 18종으로 늘렸다. 자연·문화·산책·자동차·숙소·학교·업무·
  병원·사진·음악을 추가했고 기존 raw value는 바꾸지 않아 저장 데이터와 호환된다.
- 보관함 첫 화면의 설명과 폴더별 `직접 모아 둔 장소` 같은 부가 문구를 없앴다. 예전 고정
  `받은 장소` ID는 데이터 유실 방지를 위해 내부적으로 유지하되 이름을 `미분류`로 바꾸고,
  실제 미분류 장소가 없으면 목록에 표시하지 않는다.
- 공유 익스텐션은 더 이상 복구용 미분류 폴더를 기본 선택하지 않는다. 공유 화면에서 사용자
  폴더를 명시적으로 골라야 저장할 수 있고, 앱을 열지 않고도 그 자리에서 마크·색을 골라 새
  폴더를 만들 수 있다. 앱의 장소 상세 폴더 선택 화면에서도 새 폴더를 바로 만들 수 있다.
- 사이드 메뉴의 `내 지도` 독립 항목을 없애고 보관함 안에 `내 동선`으로 넣었다. 보관함
  시트를 완전히 닫은 뒤 내 동선 시트를 열어 iOS의 중복 시트 표시 실패를 피한다. 내 동선
  화면의 제목과 결과 알림도 같은 이름으로 통일했다.
- 현재 위치는 첫 탭에서 한 번 요청하고, 위치가 확인된 뒤 두 번째 탭부터 마지막 좌표로
  카메라를 즉시 재포커스한다. 요청 중 반복 탭은 중첩 GPS 요청을 만들지 않는다.
- 관련 UI 테스트의 보관함→내 동선 진입, 확장 마크, 현재 위치 두 번째 탭 검증을 갱신했다.
  이번 요청은 Windows에서 소스만 수정했으며 Swift 빌드·iOS UI 테스트·TestFlight는 실행하지
  않았다. 다음 배포 전에 macOS CI에서 Share Extension을 포함한 컴파일과 UI 캡처 확인이 필요하다.

### 2026-08-07 웹 브랜드 `도화지` 통일 — 운영 배포

- iOS 번들의 실제 `icon-1024.png`를 웹에서도 정적 import해 랜딩, 편집기, 공유 뷰어,
  지원, 개인정보 화면에 같은 앱 아이콘과 `도화지` 이름을 표시한다.
- 브라우저 favicon·Apple touch icon·웹 앱 매니페스트에도 같은 아이콘과 `도화지` 이름을
  등록했다. 별도 복제본을 두지 않아 앱 아이콘 원본이 바뀌면 웹 빌드도 같은 파일을 쓴다.
- 공유 지도의 문서 제목, Open Graph와 Twitter 제목에 남아 있던 `MAP-LINE`을 `도화지`로
  바꾸고 OG `siteName`도 지정했다. 공유 뷰어 하단 CTA는 `도화지에서 나도 지도 만들기`다.
- 390×844 모바일 브라우저에서 랜딩과 편집기 헤더를 확인했고 아이콘 로드, 이름·지도 제목·
  저장 상태·공유 버튼 배치를 검증했다. 타입 검사, 운영 빌드, DB를 쓰지 않는 284개 테스트가
  통과했고 린트는 오류 없이 기존 nearby 테스트의 미사용 인자 경고 1건만 남았다.
- 전체 테스트의 Supabase 통합 테스트 34개는 샌드박스 네트워크에서 `fetch failed`가 났고,
  원격 DB 변경 가능성이 있는 권한 재실행은 자동 승인되지 않아 실행하지 않았다. 이번 변경과
  무관한 기존 통합 묶음이다.
- 사용자 승인에 따라 웹 변경만 선별 커밋해 `agent/ios-map-poi-context`와 `main`에 반영하고,
  Railway의 `main` 자동 배포로 운영 웹에 적용한다. 앱 소스와 TestFlight 빌드는 변경하지 않는다.

### 2026-08-07 TestFlight 33·App Store 1.0 심사 제출 완료

- 최신 소스 `54c7c68`을 `agent/ios-map-poi-context`와 `main`에 푸시. 다른 지도를 불러오면
  해당 동선 전체로 카메라가 이동하고, 장소를 단계에 담는 화면과 동선 장소 상세에서 바로
  보관함 폴더에 저장할 수 있게 했다. 보관함 마커는 22pt로 2pt 축소했다.
- 왼쪽 메뉴는 안전 영역 아래부터 내용을 배치해 시계와 제목이 겹치지 않게 했고, 제목 옆에는
  SF Symbol이 아니라 번들에 포함된 실제 도화지 앱 아이콘을 표시한다.
- iOS CI `31150005998`에서 Swift 빌드·번들 키 검사·도메인 테스트·전체 UI 테스트·스크린샷
  추출이 모두 통과했다. 사이드 메뉴 캡처에서 앱 아이콘과 상태 표시줄 간격도 확인했다.
- TestFlight 실행 `31151175196`에서 **도화지 1.0 (33)** 아카이브와 Apple 업로드에 성공했다.
  실제 앱과 Share Extension의 `CFBundleVersion`을 모두 33으로 검증한 뒤 업로드하며, 실행 중
  생성한 개발 인증서는 회수했다. 직전 실행 32는 빌드 번호가 1로 남아 Apple에서 거절됐고,
  `project.yml`과 워크플로 검증을 수정해 재발하지 않게 했다.
- App Store Connect에는 빌드 33, 한국어 메타데이터, 콘텐츠 권한 선언, 심사 메모, 수동 출시
  방식을 저장했다. 제출 스크린샷은 `appstore/screenshots/ko-KR/submission-2026-08-07/`의
  5장을 데이트 코스 → 중간지점 지도 → 중간지점 생성 과정 → 강원도 여행 → 보관함 폴더
  순서로 등록했다. 알파 채널을 제거한 RGB 1206×2622 원본은 6.3형에, 같은 내용을 RGB
  1320×2868로 변환한 사본은 6.9형 대표 슬롯에 등록했고 6.5형은 6.9형 세트를 자동 사용한다.
- API 진단 실행 `31153206231`에서 처음 올린 일부 PNG가 숨은 알파 채널 때문에
  `IMAGE_ALPHA_NOT_ALLOWED`로 실패한 사실을 확인했다. RGB 교정본 교체·처리 확인·심사 제출
  실행 `31153605824`가 성공했고, 독립 검증 실행 `31153766608`에서 6.3형 5장과 6.9형
  5장이 모두 `COMPLETE`, 앱 버전과 심사 요청이 모두 `WAITING_FOR_REVIEW`임을 확인했다.
- 심사 제출 시각은 2026-08-07 15:22 KST다. 승인 후 자동 출시가 아니라 App Store Connect에서
  사용자가 수동으로 출시해야 한다.

### 2026-08-07 iOS CI·TestFlight 31 완료 — 운영 서버 배포 대기

- 누적 소스 `2656136`과 UI 테스트 안정화 `0944516`을
  `agent/ios-map-poi-context`에 푸시
- iOS CI `31146150106`에서 Swift 빌드·번들 키 확인·도메인 테스트·UI 테스트·스크린샷
  추출까지 모두 통과
- 카카오 지도 SDK의 실제 POI 탭은 그대로 검증하되, UI 테스트의 장소 응답만 DEBUG 전용
  `-uiTestingTappedPlace` 고정값을 사용하도록 분리. 서버의 POI 선별은 웹 단위 테스트가 담당
- TestFlight `31146966867`에서 **도화지 1.0 (31)** 아카이브와 Apple 업로드 성공.
  `ARCHIVE SUCCEEDED`, `Upload succeeded`, `EXPORT SUCCEEDED`를 로그에서 확인했고 이번 CI가
  만든 개발 인증서와 API 키도 회수·삭제 완료
- 이번 요청에서는 운영 `main`과 Railway를 변경하지 않았다. 따라서 새 `/api/nearby`의 최대
  15개 내부 검사·정확한 POI 우선 처리와 웹 경로 요약 표시는 운영 서버 배포 전까지 비활성이다.
  TestFlight 31도 기존 운영 API 주소를 사용하므로 이 부분을 켜려면 `main`/Railway 배포 승인이 필요

### 2026-08-07 같은 단계 후보선·웹 경로 요약 — 소스만 반영, 빌드 전

- iOS가 문서의 `showCandidateLinks` 값을 실제 지도 렌더러에 전달하도록 연결. 후보가 둘
  이상인 단계는 각 후보를 단계 중심점으로 잇는 2pt 회색 점선과 6pt 중심점으로 표시
- 후보 보조선은 실제 이동 경로보다 아래의 전용 Shape/Label 레이어에 두고, 지도 배율이
  바뀌면 점선 간격을 화면 기준으로 다시 계산. UI 테스트 진단값에 그린 spoke 수도 추가
- 웹 편집 지도와 공유 지도에 유효한 실제 경로의 화면상 길이 중앙을 계산해
  `도보 · 1.2km · 16분` 형식의 이동수단·거리·시간을 흰 외곽선과 함께 표시
- 공유 미리보기 SVG에도 같은 계산과 글자를 적용하고, 수단 이름·거리·시간 포맷을 공통화
- 웹 전체 테스트 319개, TypeScript 타입 검사 통과. ESLint 오류는 없고 기존 nearby 테스트의
  미사용 인자 경고 1건만 남음
- 사용자 요청 흐름에 따라 Swift 빌드·UI 테스트·커밋·푸시·웹 배포·TestFlight는 실행하지 않음

### 2026-08-07 현재 위치 실기기 경로 재보강 — 소스만 반영, 빌드 전

- `CLLocationManager.startUpdatingLocation()` 지속 추적을 `requestLocation()` 일회 요청으로 변경
- 오래된 캐시만 오거나 `locationUnknown` 임시 오류가 오면 0.8초 뒤 재요청하되, 재시도마다
  연장되지 않는 20초 전체 제한을 적용. 지도 중심 용도에 맞게 요구 정확도는 100m로 조정
- 성공하면 카카오 지도의 진행 중 카메라 애니메이션을 끝내고 현재 좌표를 즉시 적용. 전국처럼
  넓은 배율이면 공통 레벨 5까지 확대하고 파란 현재 위치점을 다시 그림
- 권한 설명 키는 기존처럼 `NSLocationWhenInUseUsageDescription`을 유지하며 위치를 서버에
  저장하거나 백그라운드에서 추적하지 않음
- 사용자 요청에 따라 Swift 빌드·실기기 테스트·커밋·푸시·TestFlight는 실행하지 않음.
  다음 배포 전 실제 위치 권한 허용/거부/정확한 위치 끔 세 경우를 실기기에서 확인할 것

### 2026-08-07 카카오 기본 POI 정확도 개선 — 소스만 반영, 배포 전

- 기본 지도 마커를 탭하면 iOS가 SDK의 POI id와 좌표를 `/api/nearby`에 함께 전달
- 서버는 음식점·카페·관광명소를 카테고리별 5곳이 아니라 최대 15곳까지 한 번에 받은 뒤,
  최종 네 곳으로 자르기 전에 POI id가 정확히 일치하는 장소를 먼저 분리
- 정확한 장소 1곳이 있으면 가까운 다른 후보 3곳과 함께 반환. 현재 TestFlight 앱과의
  호환을 위해 기존 `places` 첫 항목에도 정확한 장소를 유지하고 새 응답에는 `tappedPlace` 추가
- 새 iOS는 `tappedPlace`가 있을 때만 `지도에서 누른 장소`라고 표시. id가 다르면 가장 가까운
  음식점을 거짓 확정하지 않고 `이 위치의 가까운 후보`로 표시
- `lib/kakao/nearby.test.ts` 11개, TypeScript 타입 검사와 ESLint 통과. 사용자 요청에 따라
  Swift 빌드·UI 테스트·커밋·푸시·서버 배포·TestFlight는 실행하지 않음

향후 계정 동기화는 첫 실행 회원가입 강제가 아니라, 사용자가 백업·기기 간 동기화를 켤 때만
로그인을 안내하는 선택형 흐름으로 설계할 것.

### 2026-08-07 보관 마커·동선 장소 상세 개선 — 소스만 반영, 빌드 전

- 지도에 표시되는 개인 보관함 마커를 터치할 수 있게 바꾸고, 장소명·주소·현재 폴더와
  `새 단계 만들기`·기존 단계 후보로 추가를 한 화면에서 제공
- 지도 위 동선 핀뿐 아니라 동선 편집 목록의 장소 행 전체를 누르면 같은 장소 상세 화면을
  열도록 통일. 여기에서 해당 장소를 보관함 폴더에 새로 저장하거나 다른 폴더로 이동 가능
- 동선 목록에는 이미 보관한 장소의 폴더 마크를 표시하고 VoiceOver에도 저장 여부를 전달
- `SavedPlace`와 `MapPlace` 변환을 공통화해 장소명·주소·카카오 장소 ID·좌표가 보관함과
  동선 사이를 오갈 때 유지되도록 보강. 폴더 저장 오류가 나면 선택 화면을 닫지 않고 표시
- 관련 단위/UI 테스트 코드는 보강했으나 사용자 요청에 따라 Swift 빌드·테스트·TestFlight는
  실행하지 않음. 다음 배포 전에 macOS에서 컴파일 및 관련 테스트 확인 필요

### 2026-08-07 저장·삭제 안전성 보강 — 소스만 반영, 빌드 전

- `내 지도`에서 목록 숨기기와 서버·공유 링크까지 지우는 완전 삭제를 분리하고, 완전
  삭제는 서버 성공 뒤에만 로컬 목록과 편집 토큰을 제거
- 익명 편집 토큰을 UserDefaults에서 Keychain으로 이전하되, Keychain 장애 때는 기존
  값을 남겨 편집 권한이 사라지지 않게 함
- 편집 초안·보관함·보관함 폴더·중간지점 기록을 원자적으로 쓰고 마지막 정상본을 백업.
  앱과 공유 익스텐션의 보관함 갱신은 `NSFileCoordinator`로 묶고 저장 실패를 화면에 표시
- 서버 저장 충돌(409)은 자동 저장을 멈추고 `복사본 저장`·`서버 버전 불러오기` 중 직접
  고르게 했으며, 네트워크 실패는 기기 초안을 유지하고 지수 백오프로 재시도
- 웹 문서의 자동선 표시 설정과 카메라 중심·축척을 iOS가 보존. 웹 1~14 레벨과 iOS
  네이티브 레벨을 변환하고, 예전 iOS가 남긴 네이티브 레벨도 읽을 때 보정

이 변경은 사용자의 요청대로 **소스와 테스트 코드만 수정했고 Swift 빌드·테스트·커밋·푸시·
TestFlight는 실행하지 않았습니다.** 다음 작업자는 macOS CI에서 먼저 컴파일과 관련 단위·UI
테스트를 통과시킨 뒤 배포해야 합니다.

데이터 경계는 다음과 같습니다. 공유 지도 문서는 Supabase의 `maps`·`places`·`strokes`·
`labels`·`stop_legs`에 자동 저장되지만, iOS 개인 보관함/폴더와 최근 20개의 중간지점 기록,
현재 편집 초안은 기기에만 남습니다. 현재 서버 분석 데이터는 지도별 `view_count`와 날짜별
카카오 API 호출 수뿐이며 별도 사용자 행동 이벤트나 계정 식별자는 없습니다. 따라서 운영자는
서버에 저장된 계획 지도에서 장소·단계·이동수단을 집계할 수 있지만, 실제 이동 여부나 로컬
보관함·중간지점 기록은 볼 수 없습니다.

별개로 `rate_limits.bucket`에는 요청 제한용 `기능명:IP`가 현재 원문으로 들어갑니다. 1일보다
오래된 행을 요청 중 1% 확률로 정리하므로 삭제 시점도 보장되지 않습니다. 동선 분석 데이터는
아니지만 개인정보 처리 관점의 후속 최우선 항목입니다. 다음 변경에서 서버 비밀키 기반 해시와
명시적 보존 기한/정리 작업으로 바꾸고 App Store 개인정보 답변도 다시 확인해야 합니다.

### 2026-08-07 지도 홈·보관 마커·현재 위치 개선 — TestFlight 30 반영

- 지도 오른쪽의 장소 추가·손그림·공유에서 화면 글자를 빼고, 각각 파랑·보라·청록의
  50pt 원형 아이콘으로 통일. VoiceOver 기능 이름은 유지
- 개인 보관함의 모든 장소를 폴더 색과 마크(핀·별·하트·식사·카페 등)로 지도에 항상 표시.
  보관함을 닫거나 공유 익스텐션에서 앱으로 돌아올 때 즉시 다시 읽음
- 우상단 지도 이름 편집 모달 안에 `새 지도 만들기`를 추가하고 기존 저장 후 새 지도 확인
  흐름으로 연결
- 현재 위치는 최근의 유효한 캐시를 우선 사용하고, 일회 요청의 임시 GPS 오류를 기다린 뒤
  12초 제한 안에서 갱신. 성공하면 현재 배율을 유지해 이동하고 파란 위치점을 남김

코드 커밋은 `6b2d82c`입니다. 타입 검사·린트와 macOS CI 실행 `31128516420`의 Swift 빌드,
앱 키 검증, 새 위치·보관 마커 단위 테스트, 전체 UI 회귀 테스트, 스크린샷 추출이 모두
통과했습니다. 당시 GitHub Actions 전체 장애로 push 자동 실행이 누락되고 수동 실행도 약
9분간 러너 대기했지만, 배정 뒤 앱 단계는 정상 완료했습니다. 문서 갱신 커밋 `dc7b888`까지
TestFlight `1.0 (30)`으로 업로드했습니다. TestFlight 실행 `31128988809`에서
`ARCHIVE SUCCEEDED`, `Upload succeeded`, `EXPORT SUCCEEDED`를 확인했고, 이번 CI가 만든
`Created via API` 개발 인증서 1개를 회수했으며 다른 인증서 1개는 건드리지 않았습니다.

### 2026-08-07 최신 지도 가독성 개선 — TestFlight 29 반영

- `main` 커밋 `270df35`까지 TestFlight `1.0 (29)`로 App Store Connect 업로드 완료
- 아이콘형 손그림 되돌리기·전체 지우기·공유, 앱 도보 7pt·대중교통 9pt·자전거 8pt
  경로선, 1.8배 지도 메모 글자, 대표 후보 선택 직후 경로 재탐색 등 최신 변경 포함
- TestFlight 실행 `31118478750` 3차 시도에서 `ARCHIVE SUCCEEDED`, `Upload succeeded`,
  `EXPORT SUCCEEDED` 확인
- 같은 실행에서 이번 CI가 만든 `Created via API` 개발 인증서 1개를 회수했고, 다른 인증서
  1개는 건드리지 않음
- 1차 시도는 GitHub가 macOS hosted runner를 배정하지 못해 단계 0개로 종료, 2차는
  `Set up job`의 GitHub `Service Unavailable`로 종료. 둘 다 빌드·서명·인증서 발급 전에
  실패했고 3차에서 전체 성공

### 2026-08-06 손그림·경로·메모 가독성 개선 — 아이콘 정리·심사 캡처 갱신

- 손그림 버튼의 화면상 획 수를 제거하고, 되돌리기·전체 지우기·공유는 글자 없는 원형
  아이콘으로 정리. 접근성 이름과 자동 테스트용 획 수 값은 유지
- 마지막 획은 반복해서 하나씩 되돌릴 수 있고, 전체 지우기는 5초 `실행 취소`로 복구 가능
- 실제 경로 선은 최종적으로 앱 도보 7pt·대중교통 9pt·자전거 8pt, 웹 도보 6.5px·
  대중교통 8px·자전거 7px로 굵게 조정
- 경로 중간의 이동수단·거리·시간 글자는 17→20pt, 흰 외곽선은 6→7pt로 확대
- 지도 메모는 앱 글자 배율을 1.8배, 흰 외곽선을 7pt로 강화하고, 웹·공유 썸네일도 글자를 bold로
  바꾸고 흰 배경과 테두리 대비를 높임
- 전체 지우기 실행 취소가 다른 장소·메모 변경까지 되감지 않도록 실행 취소 스냅샷을
  변경 종류별로 분리

앱 기능 코드 커밋은 `102578a`, 심사용 데이트 코스 캡처 데이터까지 포함한 최종 커밋은
`23ec4d8`입니다. 타입 검사·린트와 관련 렌더링 테스트 17개를 통과했습니다. macOS CI 실행
`31111783323`의 재시도와 최종 실행 `31116287160`은 Swift 빌드·앱 키·도메인 테스트·전체
UI 테스트·스크린샷 추출까지 모두 통과했습니다. 중간 실행 `31114681709`에서는 새 데이트·
강원도 캡처 테스트가 각각 통과했지만 기존 보관함·화면 묶음 UI 테스트 두 건이 일시적으로
실패했고, 다음 최종 실행에서 모두 통과했습니다. 이 변경은 아직 TestFlight 27에는 포함되지
않았습니다.

App Store 제출 캡처는 `appstore/screenshots/ko-KR/submission-2026-08-06/`의 5장만 사용합니다.
순서는 성수 데이트 코스와 구간별 메모, 여러 지역 친구들의 중간지점 지도, 중간지점 생성 과정,
강원도 7단계 여행 동선, 보관함 폴더입니다. 손그림·메모 단독 캡처는 핵심 쓰임이 바로 드러나지
않아 제출 목록에서 제외했습니다. 현재 iOS 워크플로는 캡처 하나를 바꿔도 전체 UI 테스트를
실행하므로, 다음에 심사 캡처를 자주 갱신한다면 지정한 `AppStoreScreenshotUITests`만 실행하는
수동 `workflow_dispatch` 작업을 분리하는 것이 macOS 사용량을 줄이는 후속 개선입니다.

### 2026-08-06 App Store 1.0 제출 준비

- iOS 마케팅 버전을 `1.0`으로 맞춘 커밋 `c990abc`를 `main`에 반영
- GitHub Actions 실행 `31103781371`에서 App Store용 아카이브·Apple 업로드·CI 임시
  인증서 회수까지 성공
- App Store Connect 앱 이름 `도화지`, 부제 `함께 만드는 모임 동선 지도`, 기본 카테고리
  `내비게이션`, 보조 카테고리 `여행`, 연령 등급 `4+`로 저장. 무료·공개 앱으로 175개
  국가/지역에 출시하도록 설정했고, 검증하지 않은 Apple Silicon Mac·Vision Pro 배포는 제외
- 개인정보 공개는 공유 지도에 저장되는 `기타 사용자 콘텐츠` 한 항목만 앱 기능 목적으로
  수집하며, 신원 연결과 추적은 하지 않는 것으로 작성
- 공개 개인정보 처리방침 `/privacy`, 지원 `/support`, 한국어 제품 페이지·심사 메모를
  운영 배포하고 App Store Connect에 저장. 개인정보 답변도 게시 완료
- App Store용 최신 `1.0 (12)` 빌드를 버전에 연결했고 승인 뒤 수동 출시하도록 설정
- iPhone 17 Pro 6.3형 제출용 스크린샷 5장을 사용자 지정 순서로 생성. 사용자 확인 후 App Store
  Connect에 업로드할 것
- 남은 일: 스크린샷 사용자 승인, App Review 연락처 입력, 남은 필수 항목 확인 후 심사 제출

### 2026-08-06 대표 경로·공유 화면 중심 수정

- 이동수단을 먼저 고른 뒤 복수 후보 단계의 대표를 정해도, 그 순간 새 대표와 맞닿은
  앞·뒤 구간의 실제 경로 탐색을 바로 시작
- 공유 웹 화면은 문서에 저장된 과거 중심점 대신 핀·메모·손그림·유효한 실제 경로 전체를
  맞춰 첫 화면을 표시
- 공유 미리보기 썸네일도 같은 범위를 사용하며, 기존 잘못 생성된 썸네일은 `scene-v2`
  캐시 경로로 자동 재생성

코드 커밋은 `74bc41b`입니다. 웹 단위 테스트 45개·타입 검사·린트·운영 빌드를 통과했습니다.
macOS CI 실행 `31099879871`의 첫 시도는 빌드·앱 키·새 대표 경로 테스트를 포함한 도메인
테스트를 통과했고, 기존 지도 롱프레스 UI 테스트의 첫 제스처 한 건만 실패했습니다. 재실행도
빌드·도메인 테스트를 통과했지만 UI 테스트 프로세스가 10분 넘게 응답 없이 멈춰 사용량을
막기 위해 중단했습니다. 이 커밋은 아직 TestFlight 27에는 포함되지 않았습니다.

### 2026-08-06 사용성 개선 묶음 — CI 통과

- 편집 변경 즉시 Application Support 초안 저장, 1.5초 뒤 서버 자동 저장
- 지도 이름·저장 상태 칩, 이름 변경, 현재 지도 저장 후 새 지도 시작
- 다른 `내 지도`를 열기 전 현재 작업 선저장
- 단계 드래그 재정렬과 그대로 남은 인접 구간의 경로 보존
- 후보·핀·메모 삭제 후 5초 `실행 취소`
- 보관함 장소 한 곳/여러 곳을 새 단계 또는 기존 단계 후보로 담기
- 검색어를 바꿔도 선택한 장소가 보이는 선택 칩
- 경로 실패 시 이동수단 유지 + `다시 시도`
- 지도 오른쪽 조작을 아이콘+글자(`장소 추가`·`손그림`·`공유하기`)로 표시
- 공유 익스텐션에 원문 조각/구분 장소/성공·실패 건수 표시
- 꾹 누르기 메뉴의 장소 검색을 중첩 시트 대신 같은 시트 안의 화면 전환으로 변경

새 파일은 `MapDraftStore.swift`, `MapTitleSheet.swift`, `CourseTargetSheet.swift`,
`UndoBanner.swift`입니다. macOS CI 실행 `31083318138`에서 빌드·앱 키·도메인 테스트·
전체 UI 테스트·스크린샷 추출까지 모두 통과했습니다.

### 2026-08-06 보관함·내 지도 관리 묶음 — TestFlight 27 반영

- 보관함 전체에서 장소 이름·주소·폴더 이름 검색
- 사용자 폴더 드래그 정렬(`받은 장소`는 맨 위 고정)
- 폴더 안 복수 선택 장소를 한 번에 다른 폴더로 이동
- `내 지도`에 공유 썸네일·지도 이름·단계 수·저장 시각 표시
- 스와이프/컨텍스트 메뉴에서 지도 전체 복제
- 지도 왼쪽 아래 현재 위치 이동 버튼(일회성 위치 요청, 지속 추적/서버 저장 없음)

코드 커밋은 `637f5c5`, macOS CI 실행 `31092748624`에서 빌드·앱 키·도메인 테스트·
전체 UI 테스트·스크린샷 추출까지 모두 통과했습니다. 앱 표시 이름을 `도화지`로 바꾼
`065fb16`까지 TestFlight 빌드 27에 포함했습니다.

이 제품의 **기본은 앱**입니다. 원래 앱으로 만들려던 것이고, 손으로 그리고 손으로 짚는
제품이라 그렇습니다. 웹은 없애지 않습니다 — 링크를 받은 사람이 아무것도 설치하지 않고
여는 자리로 계속 남습니다. 안드로이드는 한참 뒤입니다.

- 배포본(웹): https://map-line-production.up.railway.app (Railway, `ap-southeast-1`)
- 저장소: https://github.com/hiorio/MAP-LINE (`main`)
- DB: Supabase `chouiphlafpxmglwriix` (**서울 `ap-northeast-2`**)
- 마이그레이션 0001~0010 적용 완료
- 운영 서버: `main` 커밋 `b376c75` 배포 완료. 중간지점 운영 응답에서
  이동수단 가중 중심점과 도보·대중교통 실제 경로 좌표 반환 확인
- iOS 표시 이름: **도화지**. 번들 ID `com.hiorio.mapline`, 팀 `W297Z9DQ9U`는 유지
- **TestFlight 1.0 (30) 업로드 완료**. 최신 `main` 커밋 `dc7b888`까지 포함됩니다.
- TestFlight 실행 `31128988809`는 아카이브·업로드·CI 임시 인증서 회수까지 통과했습니다.
  일반 CI 실행 `31096653673`은 빌드·도메인 테스트와 UI 테스트 6개는 통과했지만 기존 메모
  롱프레스 UI 테스트 한 건에서 시뮬레이터가 첫 제스처를 인식하지 않아 실패했습니다.

⚠️ **가장 중요한 것: 실기기 확인이 하나도 안 됐습니다.** 앱 기능은 CI 시뮬레이터
스크린샷으로만 봤고, 웹은 카카오톡 인앱 브라우저에서 한 번도 안 돌려봤습니다.
아래 "다음 세션이 제일 먼저 할 일" 참고.

- iOS 상세(구조·빌드·SDK 시그니처 확인법·밟은 함정): [ios/README.md](ios/README.md)
- 장기 계획: [docs/ROADMAP.md](docs/ROADMAP.md)
- 배포: [docs/DEPLOY.md](docs/DEPLOY.md) / 마이그레이션: [docs/MIGRATIONS.md](docs/MIGRATIONS.md)

## 웹과 앱이 나눠 갖는 것

서버(`app/api/**`)와 도메인 규칙은 **한 벌만 있고 둘이 같이 씁니다.** 앱은 카카오 REST
키를 갖지 않습니다(뜯기면 남이 우리 쿼터를 태웁니다). 그래서 앱도 검색·길찾기·중간지점을
전부 우리 서버를 거쳐 부릅니다.

| API | 쓰는 곳 |
|---|---|
| `/api/search`, `/api/nearby` | 장소 검색, 꾹 누른 자리 주변 |
| `/api/route` | 도보·대중교통·자전거 실제 경로 |
| `/api/midpoint` | 중간지점 후보 |
| `/api/maps`, `/api/maps/{slug}` | 지도 생성·불러오기·저장 |
| `/api/parse-share` | 공유 텍스트에서 장소 뽑기 |
| `/api/og/{slug}` | 카톡 미리보기 썸네일 |

**같은 답을 내야 하는 규칙은 양쪽에 각각 있고 서로를 주석으로 가리킵니다.**
한쪽만 고치면 같은 지도가 두 곳에서 다르게 그려집니다.

| 규칙 | 웹 | 앱 |
|---|---|---|
| 문서 모양(키 이름) | `lib/map/types.ts` | `ios/MapLine/Domain/MapDocument.swift` |
| 대표 후보·구간 규칙 | `lib/map/legs.ts` | `ios/MapLine/Domain/StopLeg.swift` |
| 구간을 어떻게 그릴지 | `lib/render/sceneGeometry.ts` | `ios/MapLine/Domain/LegShapes.swift`, `Map/LegStyle.swift` |
| RDP 단순화 | `lib/geo/rdp.ts` | `ios/MapLine/Domain/GeoStroke.swift` |

## 실행

### 웹

```bash
cd C:\Projects\MAP-LINE
npm install   # 이미 설치돼 있으면 생략
npm run dev
```

- 랜딩: http://localhost:3000
- 편집기: 랜딩의 "지도 만들기" 클릭 (슬러그 자동 생성) 또는 임의 슬러그로 `/edit/아무값`
- W0 프로토타입(참고용, 이제 안 건드려도 됨): http://localhost:3000/prototype/draw.html

**주의:** `npm run dev`가 떠 있는 상태에서 `npm run build`를 돌리면 `.next`가 깨져서 모든 페이지가 500을 냅니다(`Cannot find module './vendor-chunks/....js'`). 겪으면:
```bash
npx kill-port 3000 && rm -rf .next && npm run dev
```

### 앱

**로컬에서 못 돌립니다. 맥이 없습니다.** 고치고 push하면 CI가 빌드·테스트하고
스크린샷을 남깁니다. 한 번에 약 10분입니다. 작업 방식이 보통과 다르므로
[ios/README.md](ios/README.md)의 "빌드와 검증 — 맥이 없다"를 **먼저 읽으세요.**

## 환경 변수 현재 값 (`.env.local`)

| 변수 | 상태 | 비고 |
|---|---|---|
| `NEXT_PUBLIC_KAKAO_JS_KEY` | ✅ 채워짐 | 편집기 지도가 뜬다 |
| `KAKAO_REST_KEY` | ✅ 채워짐 | 장소 검색·공유텍스트 파싱 동작 확인 |
| `NEXT_PUBLIC_SUPABASE_URL` | ✅ 채워짐 | 서울 리전 프로젝트. **Railway에도 같은 값이 있어야 한다** |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ⬜ 비어 있음 | 현재 코드에서 안 씀. 없어도 됨 |
| `SUPABASE_SERVICE_ROLE_KEY` | ✅ 채워짐 | 〃 |
| `NEXT_PUBLIC_SITE_URL` | ⬜ 로컬은 불필요 | **Railway에는 배포 주소가 들어가 있어야 OG 썸네일이 뜬다** |

`.env.local`은 `.gitignore`에 걸려 있어 커밋되지 않습니다. `.env.example`에 각 변수 설명이 있습니다.

## 사용자가 해줘야 하는 것 (코드로는 못 하는 일)

### A. Supabase — ✅ 완료

서울 리전(`ap-northeast-2`) 프로젝트에 마이그레이션 0001~0010이 적용돼 있고 서버 저장이
실제로 동작합니다. 뭄바이 리전에서 옮긴 뒤 통합 테스트가 23.2초 → 7.2초로 줄었습니다.

**환경 변수는 두 곳에 있습니다** — `.env.local`(로컬)과 Railway `Variables`(배포본).
Supabase 프로젝트를 바꾸면 양쪽 다 고쳐야 합니다. 코드에는 주소가 하드코딩된 곳이 없고
`lib/supabase/server.ts` 한 곳에서만 환경 변수를 읽습니다.

`NEXT_PUBLIC_SUPABASE_ANON_KEY`는 아직 비어 있는데, 현재 코드에서 쓰는 곳이 없어 문제없습니다.
뷰어(T13)가 브라우저에서 Supabase를 직접 읽게 만들 때만 필요합니다. 지금 설계에서는 뷰어도
Route Handler를 경유하면 되므로 끝까지 안 쓸 수도 있습니다.

**2026-08-05 Security Advisor 메일의 대상은 앱 테이블이 아니라
`public.spatial_ref_sys` 한 개입니다.** 0001이 PostGIS를 스키마 지정 없이 설치하면서 생긴
좌표계 사전입니다. `maps`/`places`/메모·동선 데이터의 RLS 누락이 아닙니다. RLS를 켜는 0011을
시도했지만 이 테이블 소유자가 PostGIS 관리 역할이라 CLI가
`must be owner of table spatial_ref_sys`로 거부했고 트랜잭션은 롤백됐습니다. 실패하는 0011은
저장소에 남기지 않았습니다. 기존 의존성을 가진 PostGIS는 바로 다른 스키마로 옮길 수 없으므로,
경고까지 없애려면 Supabase Support에 확장을 `extensions`/전용 스키마로 이전해 달라고 요청해야
합니다. 표준 좌표계 메타데이터라 사용자 데이터 유출 징후는 아니고, 앱은 anon key도 쓰지 않습니다.

### B. Kakao REST API 키 — ✅ 완료

`KAKAO_REST_KEY` 설정됨. 실제 카카오 응답으로 검색·공유텍스트 파싱 모두 동작 확인.

### C. 마이그레이션 — ✅ CLI로 자동화됨

새 마이그레이션이 생기면 **`npm run db:push`** 한 줄이면 됩니다. 절차와 주의사항은
[docs/MIGRATIONS.md](docs/MIGRATIONS.md)에 있습니다. 이 PC에는 로그인·연결이 끝나 있고,
새 PC에서만 `supabase login` → `npm run db:link`를 한 번 하면 됩니다.

| 파일 | 내용 |
|---|---|
| `0001_init.sql` | 테이블·인덱스·RLS 초기 정의 |
| `0002_document_rpc.sql` | 읽기/쓰기 RPC, 0001의 `edit_token` 노출 구멍 차단 |
| `0003_view_count.sql` | 조회수, `updated_at` 트리거를 조회수에 둔감하게 |
| `0004_og_cache.sql` | OG 썸네일 캐시 컬럼, 트리거 제외 목록에 OG 컬럼 추가 |
| `0005_api_usage.sql` | 카카오 API 사용량 집계 테이블 |
| `0006_upsert_save.sql` | 저장을 업서트로 전환, 클라이언트 id 보존 |
| `0007_stops.sql` | 단계·후보 모델(`places.stop_index`), `segments` 제거 |
| `0008_auto_line_toggles.sql` | 자동 선 표시 설정 두 개 |
| `0009_rate_limit.sql` | 요청 빈도 제한 집계 |

**마이그레이션은 두 번 돌려도 안전해야 합니다.** 도구가 재실행할 수 있게 된 이상
사람이 고르는 안전장치가 사라졌습니다. 특히 데이터 백필이 위험합니다 — `0007`의
`stop_index` 백필은 재실행하면 한 단계의 후보들을 쪼개 놓는 형태였고, 널 허용 컬럼으로
만든 뒤 널만 채우는 방식으로 고쳤습니다.

스키마를 바꾼 배포는 **`db:push` 먼저, 배포 나중**입니다. 순서가 바뀌면 새 코드가
아직 없는 컬럼을 찾습니다.

### D. 배포 — ✅ 완료

Railway에 올라가 있습니다: https://map-line-production.up.railway.app

절차와 함정(포트 불일치로 인한 502, Node 버전, `npm ci`와 빌드 캐시 충돌)은
[docs/DEPLOY.md](docs/DEPLOY.md)에 정리돼 있습니다.

**Railway `Variables`에 있어야 하는 값** — `.env.local`과 같되 `NEXT_PUBLIC_SITE_URL`이
배포 주소로 들어가 있어야 카카오톡 미리보기 썸네일이 뜹니다.

카카오 콘솔 > 앱 > 플랫폼 키 > JavaScript 키의 SDK 도메인에 배포 주소가 등록돼 있어야
지도 타일이 보입니다.

> 참고: 카카오 REST/네이티브 앱 키가 작업 중 대화 로그에 평문으로 남은 적이 있습니다.
> 사용자는 신경 쓰지 않기로 했으므로 그대로 진행하되, 공개 서비스로 갈 때 재발급하면 됩니다.

## 지금까지 만들어진 것

### 웹 — 코드·테스트 있고 실제 환경에서 확인됨

| 영역 | 파일 |
|---|---|
| RDP 단순화·좌표 투영·줌 보정 | `lib/geo/rdp.ts`, `lib/geo/projection.ts` |
| 카카오 SDK 로더·지도 | `lib/kakao/loadSdk.ts`, `components/map/KakaoMap.tsx` |
| 캔버스 오버레이 (손그림·핀·라벨·화살표) | `components/map/MapOverlay.tsx`, `components/map/useMapCanvas.ts`, `lib/render/scene.ts` |
| 편집기 상태·되돌리기 | `store/useMapStore.ts` |
| 단계·후보 모델 | `lib/map/types.ts`(`Stop`, `flattenStops`, `stopCentroid`), `0007` |
| 자동 선(후보 연결선·이동 화살표)과 끄기 | `lib/render/scene.ts`, `0008` |
| 라벨 수정·이동 | `components/map/MapOverlay.tsx` (탭=수정, 드래그=이동) |
| 장소 검색·거리순 정렬 | `lib/kakao/localSearch.ts`, `lib/kakao/searchParams.ts`, `app/api/search` |
| 공유 텍스트 파싱 | `lib/kakao/parseShareText.ts`, `app/api/parse-share` |
| 보관함(개인 장소 저장) | `lib/map/savedPlaces.ts`, `store/useSavedPlacesStore.ts` |
| 서버 저장·낙관적 잠금 | `app/api/maps/**`, `lib/map/persistence.ts`, `0002`/`0006` |
| 뷰어·공유 링크 | `app/m/[slug]/**`, `Editor.tsx`의 `ShareButton` |
| OG 썸네일 + Storage 캐시 | `app/api/og/[slug]`, `lib/map/ogImage.ts`, `lib/kakao/staticMap.ts`, `0004` |
| 썸네일에 핀·손그림·화살표·메모 합성 | `lib/render/ogOverlay.ts`, `lib/render/sceneGeometry.ts`, `lib/map/staticProjection.ts`, `nixpacks.toml` |
| 실제 경로 동선(도보·대중교통·자전거) | `lib/kakao/routing.ts`, `app/api/route`, `lib/map/legs.ts`, `app/edit/[slug]/useLegRoutes.ts`, `0010` |
| 랜딩 | `app/page.tsx`, `components/DemoMap.tsx` |
| 사용량 집계·진단 | `lib/kakao/usage.ts`, `app/api/usage`, `0005` |
| 요청 빈도 제한 | `lib/rateLimit.ts`, `0009` |

**실제 환경 검증 요약**

| 확인 | 결과 |
|---|---|
| 서버 저장 왕복 | 제목·단계·후보 순서·좌표·설정 보존 (통합 테스트 26개) |
| 클라이언트 id 보존 | 여러 번 저장해도 불변, 교차 지도 id 탈취 차단 |
| 낙관적 잠금 | 오래된 `updatedAt` → 409 |
| `updated_at` 트리거 | 조회수·썸네일 기록에 반응하지 않음 |
| 배포본 전 기능 | 생성·저장·뷰어·OG(216KB)·삭제 모두 200 |
| 요청 빈도 제한 | 20건까지 통과, 21건째 429, 다른 IP는 영향 없음 |
| 서울 리전 이전 | 통합 테스트 23.2초 → 7.2초 |

`npm run typecheck` / `npm run lint` / `npm test`(**310개**) 전부 통과.
`npm run build`는 dev 서버를 끄고 돌려야 합니다(위 "실행" 주의사항).

### iOS 앱 — 웹 기능을 전부 따라잡았습니다

구조·빌드·함정은 [ios/README.md](ios/README.md)에 있습니다. 여기서는 상태만 씁니다.

| | 웹 | 앱 |
|---|---|---|
| 핀 찍기·단계·단계별 복수 후보·대표 후보 | ✅ | ✅ |
| 경로 탐색(도보/대중교통/자전거) | ✅ | ✅ |
| 지도 위 메모 | ✅ | ✅ |
| 저장·불러오기·공유 링크 | ✅ | ✅ |
| 보관함 | ✅ | ✅ |
| 중간지점 찾기 + 검색 기록 + 지도 표시 | ✅ | ✅ |
| 공유 익스텐션(다른 앱 → 보관함) | — | ✅ |
| 손그림 | ❌ | ✅ (한 획 되돌리기·전체 지우기) |

**웹에만 있고 앱에 없는 기능은 현재 없습니다.**

앱 지도 오른쪽의 `장소 추가`에서 웹처럼 검색 결과를 여러 곳 체크하고 `새 단계` 또는
기존 `N단계`를 골라 한 번에 담을 수 있습니다. 동선 화면의 `이 단계에 후보 추가`도 같은
복수선택 시트를 해당 단계가 선택된 상태로 열며, 단계 안의 후보별 대표 지정·삭제도 목록에
바로 보입니다. 기존 단계에 담으면 단계 수와 id는 유지되고 후보 배열만 늘어납니다.

지도에서 장소를 꾹 눌러 `핀 찍기`를 선택하는 흐름도 같은 단계 규칙을 씁니다. 첫 장소는
곧바로 1단계가 되지만, 단계가 하나라도 있으면 고른 장소를 `새 단계 만들기` 또는 각
`N단계에 후보로 추가` 중 어디에 담을지 선택합니다.
핀 컨텍스트의 주변 장소·단계 선택과 저장된 핀 상세의 작업 행은 모두 전체 폭에 직사각형
터치 영역을 잡았습니다. UI 테스트는 기본 POI 행의 글자 밖 오른쪽 여백을 눌러 실제로
단계가 추가되는지 확인합니다.

꾹 누르기 메뉴의 자동 후보는 음식점·카페·관광명소뿐이라 예식장 같은 시설이 빠졌습니다.
이제 좌표의 도로명 주소에 건물명이 있으면 그 이름을 자동 검색해 먼저 보여 주고, 그래도
없으면 메뉴의 `검색`에서 누른 지점 가까운 장소를 이름으로 찾을 수 있습니다. 단계 핀은
32pt → 28pt → 20pt(이름 22pt → 20pt → 16pt), 롱프레스는 UIKit 인식기로 0.45초에 열립니다.

지도 위 메모는 탭해서 내용을 수정하거나 삭제할 수 있습니다. 기본 이동 방식은 메모
글자를 0.45초간 누른 뒤 그대로 끄는 것입니다. SDK가 사용자 POI 드래그 이벤트를 주지
않아 UIKit 롱프레스 인식기로 시작 메모를 판별하고, 드래그 중에는 지도 팬을 잠근 채 POI만
손가락을 따라 움직입니다. 손을 뗄 때 같은 id의 모델 좌표를 한 번 확정합니다. 기존
`위치 옮기기` 버튼은 드래그가 어려운 경우의 보조 경로로 유지합니다.

손그림 모드를 켜도 오른쪽 버튼에는 `손그림`만 보이고 획 수는 숨깁니다. 바로 아래에
글자 없는 되돌리기·휴지통 아이콘이 나란히 나타나며 공유도 원형 아이콘만 표시합니다.
되돌리기는 마지막 획을 하나씩 반복 제거하고, 전체 지우기 뒤에는 5초 동안 `실행 취소`로
모든 획을 되살릴 수 있습니다. 평소에는 이 도구를 숨겨 지도 조작부가 복잡해지지 않습니다.

중간지점은 참가자별 이동수단을 후보 압축부터 반영합니다. 느린 도보 참가자 쪽으로 검색
중심을 당기고, 직선거리 대신 수단별 예상시간으로 결선 5곳을 고른 뒤 실제 도보·대중교통·
자전거 경로 시간으로 최종 3곳을 정렬합니다. 앱 결과에서는 1·2·3순위를 여러 개 체크해
한 지도에 함께 올릴 수 있고, 서버가 반환한 실제 경로 좌표를 수단별 선으로 그립니다.
경로 조회에 실패한 참가자만 흐린 직선으로 표시합니다.

길찾기로 만든 일반 동선 선의 길이 중간에는 `도보 · 1.2km · 16분` 형식으로 수단·거리·
시간을 표시합니다. 중간지점 참가자별 선에는 이름도 앞에 붙입니다. 좌표 배열 인덱스의
가운데가 아니라 전체 선분 길이의 50% 지점을 계산하므로 한쪽에 좌표가 몰려도 치우치지
않습니다. 일반 동선은 자동차도 고를 수 있지만, 아래 정책 제약 때문에 경로 결과는 저장하지
않고 편집/조회 세션에서만 실시간으로 표시합니다. 중간지점 참가자도 도보·대중교통·자전거·
자동차를 고를 수 있으며, 자동차 경로가 포함된 기록은 다시 계산이 필요하다고 표시합니다.

다른 앱 공유는 한 텍스트를 한 장소로 취급하던 구조를 고쳤습니다. 주소 줄을 기준으로
최대 10곳을 분리하고, 정확한 주소는 Kakao 주소 검색으로 먼저 좌표화한 뒤 장소명 키워드
후보를 보완합니다. 공유 익스텐션은 장소별 후보를 하나씩 고른 뒤 선택한 여러 곳을 한 번에
선택한 보관함 폴더에 담습니다. `naver.me`/`kko.kr` 단축 URL 자체는 여전히 풀지 않습니다.

보관함은 이제 공유로 받은 장소에 국한되지 않습니다. `받은 장소` 기본 폴더와 사용자 폴더가
있고, 폴더마다 핀·별·하트·맛집·카페·여행·쇼핑·생활 마크와 색을 정합니다. 폴더 안에서
장소를 직접 검색해 넣고, 다른 폴더로 이동·삭제하거나 동선 단계로 올릴 수 있습니다.
기존 보관함 JSON은 `groupID`가 없으면 `받은 장소`로 읽어 데이터가 사라지지 않습니다.

공유 문서에는 원래 단계와 복수 후보가 모두 들어갔지만, 웹 뷰어가 하단 칩에서 첫 후보만
보이고 나머지를 `외 N곳`으로 접은 채 펼칠 화면이 없었습니다. 공유 페이지 상단의
`모임 동선`에서 이제 단계별 모든 후보·대표/대표 미정·다음 이동수단·거리·시간·대중교통
안내를 볼 수 있고, 후보를 누르면 지도에서 그 위치를 봅니다. 카카오톡 OG 설명도 첫
후보만 나열하지 않고 단계별 복수 후보를 포함합니다.

배포된 서버에 실제 문서를 왕복시켜 계약을 확인했습니다 — 제목·장소명·메모 한글 무손상,
단계·구간·획 색·굵기 모두 보존. 확인 중에 **서버가 UUID가 아닌 id는 새로 부여한다**는
것을 발견했는데, 그러면 저장했던 경로가 "끝점이 바뀌었다"로 버려집니다. 앱은
`UUID().uuidString`을 쓰므로 해당 없고, UUID를 보내면 id와 경로가 그대로 유지되는
것까지 확인했습니다. **직접 만든 id로 저장하는 코드를 새로 쓰면 이걸 다시 확인하세요.**

### 미완료

- **실기기 확인 전무** — 앱·웹 둘 다. 아래 "다음 세션이 제일 먼저 할 일" 참고
- **Universal Links** — `/m/{slug}` 링크를 탭하면 웹이 열립니다. 앱이 열리게 하려면
  서버에 `apple-app-site-association`이 필요합니다. 지금도 정상 동작이라 별건으로 남겼습니다
- **앱에서 지도 제목을 지을 자리가 없습니다** — 첫 단계 이름을 제목으로 씁니다
  (`ContentView.currentDocument()`). 목록에서 알아볼 수는 있지만 사용자가 정한 이름은 아닙니다
- **자동차 동선** — 도보·대중교통·자전거는 됩니다. 자동차만 뺐습니다. 이유는 아래
  "길찾기" 항목 참고
- **신고 링크** — 빈도 제한은 넣었지만 신고 창구는 없습니다
- **안드로이드** — 손 안 댐. 한참 뒤로 미뤄 둔 것입니다

### 운영 DB에 남아 있는 테스트 지도

계약 확인용으로 만든 것들이라 지워도 됩니다. 사용자에게 물어보고 지우세요.

- `9ms6ywt9`, `rtjh7vbv` — 제목이 깨져 있습니다. **PowerShell이 요청 본문을 ANSI로
  보내서 생긴 것**이고 앱/서버 문제가 아닙니다. API를 curl 아닌 PowerShell로 시험할 때
  한글이 깨지면 이걸 떠올리세요
- `yx65y74h` — 정상. 앱 문서 계약 확인용

## 다음 세션이 제일 먼저 할 일

**실기기에서 확인.** 코드는 다 됐지만 실제 손가락으로는 한 번도 안 돌려봤습니다.
앱은 CI 시뮬레이터 스크린샷으로만, 웹은 데스크톱 브라우저로만 봤습니다.

### A. iOS 앱 (TestFlight)

**TestFlight 1.0 (30)을 App Store Connect에 업로드했습니다.** Apple 처리 완료 후
`맵라인테스트` 내부 그룹에서 설치 가능 여부를 확인하고 아래 항목을 실기기에서 점검할 것:

- 지도를 **꾹 눌렀을 때** 0.45초가 적당한가. 지도를 끌려다 뜨지는 않는가
- 스타벅스처럼 지도에 보이는 기본 장소 마커를 한 번 탭하면 핀 컨텍스트가 바로 열리고,
  `지도에서 누른 장소`가 주변 후보보다 위에 표시되는가
- 핀 컨텍스트에서 장소명 글자 밖의 행 여백을 눌러도 장소 선택이 진행되는가
- 기본 장소 마커를 길게 눌렀을 때도 컨텍스트가 한 번만 열리는가
- 지도 오른쪽에 중간지점 버튼이 없어지고, 사이드 메뉴의 중간지점 찾기로만 진입하는가
- 보관함에서 폴더를 만들고 마크·색을 고른 뒤 장소를 직접 검색해 넣을 수 있는가
- 보관함 장소를 다른 폴더로 옮기고, 동선에 올렸을 때 폴더 색이 핀에 반영되는가
- 다른 앱 공유 화면에서 받을 폴더를 고를 수 있고 기존 공유 장소는 `받은 장소`에 남는가
- 예식장처럼 음식점·카페·관광명소가 아닌 곳을 누르면 건물명이 뜨는가. 없으면 `검색`으로 찾을 수 있는가
- 20pt 단계 핀과 16pt 이름표가 실제 화면에서 너무 작거나 크지 않은가
- 메모를 길게 누른 채 새 자리로 끌고 손을 떼면 지도는 움직이지 않고 메모만 정확히 옮겨지는가
- 새 장소 꾹 누르기 → `핀 찍기` → `새 단계 만들기`와 기존 `N단계에 후보로 추가`가 모두 보이고 선택대로 담기는가
- `장소 추가` 또는 동선의 `이 단계에 후보 추가` → 복수 선택 → 같은 번호의 핀이 둘 이상 생기는가
- 후보가 여럿인 단계에서 대표를 정하면 실제 경로가 다시 그려지는가
- 손그림 손맛 — 굵기, 따라오는 속도
- 손그림 모드의 되돌리기 아이콘을 여러 번 눌렀을 때 마지막 획부터 지워지는가
- `전체 지우기` 뒤 5초 `실행 취소`로 모든 획이 복구되는가
- 굵어진 실제 경로와 커진 이동수단·거리·시간 글자가 너무 두껍거나 다른 지도 글자를
  과도하게 가리지 않는가
- 메모의 진한 흰 외곽선이 지도 위에서 잘 읽히면서도 번져 보이지 않는가
- 확대·축소 중에 선이 지도를 제대로 따라오는가 (웹에서 제일 불만이던 부분)
- 홈 화면 앱 이름과 사이드 메뉴 제목이 `도화지`로 보이는가
- 다른 앱(카카오맵 등)에서 **공유 → 보관함에 담기**가 보이는가, 장소가 보관함에 담기는가
- 공유 버튼 → 링크가 카톡으로 가는가, **미리보기에 썸네일이 뜨는가**
- 중간지점: 사람 둘 이상 넣고 찾기 → 지도에 참가자·후보·선이 그려지는가
- 중간지점: A=도보, B/C=대중교통으로 찾았을 때 도보 참가자의 부담이 후보 순위에 반영되는가
- 중간지점: 1·2순위를 함께 체크하면 후보 핀 둘과 각 참가자의 실제 도로/철도 경로가 함께 보이는가
- 중간지점: 검색 직후 상단 기록에 남고, 다시 열면 참가자·이동수단·후보가 그대로 복원되는가
- 다른 앱에서 주소가 포함된 장소 3곳 이상을 한 번에 공유하면 전부 별도 항목으로 잡히는가

### B. 웹 — 카카오톡 인앱 브라우저

공유 링크 대부분이 카카오톡 안에서 열리는데 그 웹뷰의 pointer event 처리가 표준과
달라, 여기서 드로잉이 깨지면 제품이 성립하지 않습니다.

1. 폰 카카오톡에서 자신에게 https://map-line-production.up.railway.app 전송
2. 카톡 안에서 링크를 눌러 인앱 브라우저로 열기
3. 확인할 것:
   - 공유 페이지의 `모임 동선`에 한 단계의 후보가 모두 나오고, 대표 미정 상태가 보이는가
   - 카카오톡 링크 미리보기에 첫 후보만 확정 장소처럼 나오지 않는가
   - 손가락으로 선이 그어지는가 (제일 중요)
   - 그리기 모드에서 지도가 안 움직이는가
   - 선이 흐리지 않은가 (레티나 DPR)
   - `📍 핀` / `T 라벨` 탭 시 입력창이 뜨고 **사라지지 않는가**
     (데스크톱에서 같은 증상을 겪고 고친 적이 있음 — 터치는 또 다른 경로)
   - 라벨 드래그로 이동이 되는가
   - 편집기에서 `공유` → 카톡 전송 → **미리보기에 지도 썸네일이 뜨는가**

깨지는 게 있으면 그 화면에서 무슨 일이 일어나는지부터 알려 주세요. 원격 디버깅이
어려우므로 증상 서술이 유일한 단서입니다.

### 그다음으로 값어치가 큰 것

1. **실기기 사용성 확인** — 손그림 편집 도구·굵어진 경로·메모 외곽선을 실제 손가락과 화면으로 확인
2. **Universal Links** — 링크를 앱으로 받는 것. 앱이 기본인 제품이니 언젠가는 해야 합니다

### 아직 확인 안 된 것

- `0002`가 `0001`의 RLS 구멍을 막았는지 **직접 테스트는 못 했습니다.** 논리적으로는 확실합니다
  (0002가 통째로 성공했으므로 맨 위 `drop policy` 문들도 실행됐고, Supabase SQL Editor는
  스크립트를 한 트랜잭션으로 돌립니다). 확실히 하려면 Supabase 대시보드 > Authentication >
  Policies 에서 `maps`/`places`/`strokes`/`labels`/`segments`에 정책이 없는지 보면 됩니다.
  `NEXT_PUBLIC_SUPABASE_ANON_KEY`를 채운다면 anon으로 `select edit_token from maps`가
  거부되는지 쏴 보는 게 가장 확실합니다.

### 테스트 돌리는 법

```bash
npm test                                                  # 전체 177개
npx vitest run lib/map/mapDocument.integration.test.ts     # 스키마 건드린 뒤엔 이것부터
```

통합 테스트는 실제 Supabase에 `zztest`로 시작하는 지도를 만들고 `afterAll`에서 지웁니다.
환경 변수가 없으면 스스로 스킵합니다.

**통합 테스트를 새로 쓸 때 주의:** 지도마다 새 uuid를 쓰세요(`makeSample()`). 고정 id를 여러
테스트가 공유하면 두 번째 지도부터 교차 지도 방어에 걸려 행이 조용히 빠집니다. 실제로 겪었습니다.

**앱 쪽은 로컬에서 못 돌립니다.** 맥이 없으므로 push하고 CI를 봅니다.

```bash
gh run list --workflow=ios.yml --limit 1
gh run view <id> --json status,conclusion
gh run view <id> --json jobs -q '.jobs[].steps[] | select(.conclusion=="failure") | .name'
gh run download <id> -n ui-screenshots -D shots   # screenshots/manifest.json에 이름↔파일 대응
```

한 번에 약 10분입니다. `ios/README.md`의 "빌드와 검증 — 맥이 없다"를 먼저 읽으세요.

작업 커밋은 의미 있는 단위로 나눠서 하세요. `.env.local`은 `.gitignore`에 걸려 있지만, 새 파일을 추가하기 전에 `git status`로 한 번 확인하는 습관을 들이면 안전합니다.

## 코드에 이미 반영된, 다시 겪지 않아도 될 문제들

작업 중 실제로 겪고 고친 것들입니다. README에도 있지만 여기 요약합니다 — 재작업 방지용.

- **카카오 지도 컨테이너에 `z-index: 0`이 없으면 그리기가 안 됩니다.** SDK가 컨테이너 안에 만드는 내부 레이어가 z-index를 갖고 있어서, 컨테이너가 `auto`면 그 자식이 드로잉 캔버스보다 위에 깔려 `pointerdown`이 캔버스에 안 닿습니다. `KakaoMap.tsx`에 `z-0`, 오버레이 캔버스에 `z-10`으로 이미 고정돼 있습니다.
- **합성 이벤트로 한 검증을 믿지 마세요.** `pointerdown`만 dispatch하면 뒤따르는
  `mouseup`이 없어서, 실제 마우스에서만 드러나는 경로를 통째로 건너뜁니다. 핀·라벨 입력창이
  뜨자마자 스스로 닫히던 버그를 그렇게 놓쳤습니다. 입력이 얽힌 기능은 `computer` 도구의
  진짜 클릭·드래그로 확인하세요.
- **한글 입력 확정 Enter를 제출로 받으면 안 됩니다.** 조합을 확정하는 Enter도 `keydown`으로
  들어와 마지막 글자가 빠진 채 저장됩니다. `e.nativeEvent.isComposing`으로 걸러냅니다.
  한국어 장소 이름을 치는 제품이라 치명적입니다.
- **오류를 삼키고 빈 결과를 200으로 돌려주지 마세요.** `/api/usage`가 그래서 "DB 죽음"과
  "사용량 0"을 구분 못 했고, 배포본이 삭제된 프로젝트를 보고 있는데도 정상으로 읽혔습니다.
  상태를 보러 오는 경로는 상태를 숨기면 안 됩니다.
- **환경 변수는 두 곳에 있습니다** — `.env.local`과 Railway `Variables`. 한쪽만 바꾸면
  로컬은 되는데 배포본만 깨지고, 원인이 눈에 안 띕니다.
- **`crypto.randomUUID()`를 직접 쓰면 안 됩니다.** 보안 컨텍스트에서만 존재해서 `http://192.168.x.x:3000`(실기기 테스트용 LAN 주소)에서 터집니다. `lib/id.ts`의 `createId()`를 씁니다 — 대체 경로에서도 uuid v4 형식을 지킵니다.
- **`@next/env`를 ESM 설정 파일에서 명명 import하면 조용히 실패합니다.** CommonJS 모듈이라 에러 없이 값만 안 들어옵니다. `vitest.config.ts`는 `.env.local`을 직접 읽습니다.
- **vitest의 `setupFiles`에서 `process.env`를 채워도 워커에 반영되지 않습니다.** 설정 파일에서 읽어 `test.env`로 넘겨야 합니다.
- **`confirm()`에 의존하면 안 됩니다.** 인앱 브라우저(카카오톡 등)나 대화상자 차단 상태에서 조용히 `false`를 반환해 버튼이 안 먹습니다. "전체 지우기"는 두 번 누르기 패턴으로 대체했습니다 (`components/toolbar/EditorToolbar.tsx`의 `ClearButton`).
- **되돌리기는 pop이 아니라 스냅샷입니다.** 설계안 원문은 "pop 방식"이라 했지만, 지우개가 중간 획/핀을 지울 수 있어서 단순 pop은 엉뚱한 걸 되살립니다. `useMapStore.ts`의 `commit()`이 매 변경 전 `{places, strokes, labels}` 스냅샷을 히스토리에 쌓습니다.
- **RDP는 위경도가 아니라 화면 좌표에서 합니다.** 고정 줌에서 화면↔위경도가 국소적으로 선형이라 결과가 사실상 같고, 오차 단위가 픽셀이라 튜닝이 직관적입니다. 대신 획마다 `zoomCreated`를 저장해 다른 줌에서 볼 때 `strokeRenderWidth`/`strokeRenderAlpha`(`lib/geo/projection.ts`)로 굵기·투명도를 보정합니다.
- **팬 중엔 캔버스를 통째로 `transform`으로 밀고, 줌 중엔 숨깁니다.** 획/핀 전부를 매 프레임 재투영하면 느립니다. `idle` 이벤트에서만 정확히 재계산합니다 (`MapOverlay.tsx`).
- **핀·연결선·라벨·손그림은 캔버스 하나에서 그립니다.** 설계안은 `PinLayer`/`SegmentLayer`를 별도 컴포넌트로 뒀지만, 레이어마다 캔버스를 나누면 팬/줌 동기화를 그 수만큼 반복해야 해서 하나로 합쳤습니다 (`lib/render/scene.ts`가 그리기 함수, `MapOverlay.tsx`가 입력 처리).
- **Kakao Local API는 `x`=경도, `y`=위도입니다.** 순서가 직관과 반대라 `lib/geo/projection.ts`의 `fromKakaoXY()` 한 곳에 가둬뒀습니다. 새로 카카오 응답을 다루는 코드를 쓸 때 직접 `{lat: y, lng: x}`를 쓰지 말고 이 함수를 거치세요.
- **`updated_at` 트리거는 "내용이 바뀐 시각"만 담아야 합니다.** 0001의 트리거는 update마다 무조건 `now()`로 덮었습니다. 그대로 두면 (a) 뷰어 조회로 `view_count`가 오를 때, (b) 썸네일 URL을 기록할 때도 `updated_at`이 밀립니다. (a)는 편집기의 낙관적 잠금이 실제 충돌도 아닌데 409를 뱉게 만들고, (b)는 `og_updated_at < updated_at`이 영원히 참이 되어 **썸네일 무한 재생성 = 카카오 쿼터 소진**을 일으킵니다. 0003·0004에서 `view_count`, `og_image_url`, `og_updated_at`을 제외하도록 고쳤습니다. **메타데이터 컬럼을 새로 추가하면 이 목록에도 넣으세요** (`0004_og_cache.sql`의 `ignored` 배열).
- **OG 이미지는 `og:image` URL이 절대 경로여야 크롤러가 읽습니다.** `app/layout.tsx`의 `metadataBase`가 `NEXT_PUBLIC_SITE_URL`로 그 기준을 잡습니다. 배포 시 이걸 안 바꾸면 썸네일이 localhost를 가리켜 카톡 미리보기가 비어 보입니다.
- **카카오 정적 지도의 `markers` 파라미터는 쓰면 안 됩니다.** 폴리라인 파라미터가 아예 없는 것도 문제지만, 더 나쁜 건 `markers`를 넘기면 카카오가 **`center`를 무시하고 마커에 맞춰 지도를 다시 잡는다**는 점입니다(마커 하나면 그 마커가 무조건 이미지 한가운데에 옵니다). 지도를 만든 사람이 맞춰 둔 화면이 통째로 어긋납니다. 그래서 지도는 표시 없이 받고 핀·화살표·손그림·메모를 전부 직접 합성합니다 — `lib/render/ogOverlay.ts`(SVG 생성) + `lib/map/ogImage.ts`(sharp 합성).
- **좌표 → 픽셀 대응은 실측해서 `lib/map/staticProjection.ts`에 넣어뒀습니다.** 카카오가 문서로 내놓지 않아 직접 쟀습니다. 성질이 중요한데, **메르카토르가 아닙니다**: 위도 1도당 픽셀 수가 위도와 무관하게 일정하고, 경도만 `cos(위도)`배로 좁아지는 등거리 투영입니다. 레벨이 1 오를 때마다 정확히 절반이 됩니다. 검증은 정적 지도의 `center`를 알려진 각도만큼 옮겼을 때 이미지가 실제로 몇 픽셀 밀리는지 대조했고, 레벨 1·2·3·5·7에서 240px 기준 오차 0px이었습니다. **요청한 레벨과 그린 레벨이 어긋나면 획이 통째로 엉뚱한 배율로 찍히므로 `clampLevel()`을 양쪽에서 같이 쓰세요.**
- **도보·대중교통·자전거는 카카오맵 REST(`dapi.kakao.com/v2/routing`), 자동차는 Kakao Mobility Directions(`apis-navi.kakaomobility.com/v1/directions`)를 씁니다.** 자동차는 과거 공식 답변상 결과의 자체 DB 저장이 금지되어 `mode: car`만 저장하고 좌표·거리·시간은 현재 세션에서만 씁니다. 서버 저장 전 제거와 DB trigger를 모두 유지하세요. 공유 웹은 모든 페이지 조회가 아니라 사용자가 `모임 동선`을 연 경우에만 자동차 경로를 호출합니다. 이를 영구 저장 방식으로 바꾸려면 카카오모빌리티에서 저장 허용을 다시 명시적으로 확인해야 합니다.
- **경로 캐시는 신선도 유지가 조건입니다.** 카카오 개발자 운영정책 제5조 20호는 캐시를 금지하지 않지만, 사용자 환경 개선 목적일 것과 최신 상태로 유지할 것을 요구합니다. 그래서 `stop_legs`에 `from_place_id`/`to_place_id`/`fetched_at`을 함께 남기고, 편집기가 열릴 때 끝점이 어긋났거나 7일(`ROUTE_TTL_DAYS`)이 지난 구간을 다시 받습니다. **길찾기는 편집기에서만 부릅니다.** 조회할 때마다 부르면 인기 있는 지도 하나가 하루치 쿼터를 다 먹습니다.
- **경로의 출발·도착은 중간지점이 아니라 대표 후보입니다.** 중간지점은 후보들의 평균이라 건물도 길도 아닌 가상의 점이고 길찾기를 시작할 수 없습니다. 후보가 하나뿐인 단계는 그것이 곧 대표이고, 여럿인데 대표를 안 정했으면 경로를 그리지 않고 직선으로 둡니다. "아직 안 정함"은 지워야 할 상태가 아니라 이 제품이 표현하려는 상태입니다.
- **썸네일의 한글은 서버 글꼴에 달려 있습니다.** SVG 안에 가게 이름과 메모가 한글로 들어가는데 기본 빌드 이미지에는 CJK 글꼴이 없습니다. `nixpacks.toml`의 `fonts-noto-cjk`가 그걸 넣습니다. 이 줄이 빠지면 **핀과 선은 멀쩡한데 글자만 빈칸/두부로 나와서** 알아채기 어렵습니다.
- **0001의 RLS 정책에는 보안 구멍이 있었고 0002에서 막았습니다.** `create policy maps_read on maps for select using (true)`는 컬럼 단위가 아니라서 anon 키로 `select edit_token from maps`가 통했습니다. 즉 누구나 남의 지도를 편집할 수 있었습니다. `maps_public` 뷰로는 원본 테이블 직접 조회를 막지 못합니다. 0002에서 읽기 정책을 전부 없애 기본 거부로 만들고, `get_map_document`(security definer, edit_token 미반환)로만 읽기를 엽니다. **새 테이블에 읽기 정책을 추가할 때 같은 실수를 반복하지 마세요.**
- **저장은 지도 전체 스냅샷을 보내지만 DB에는 업서트로 반영됩니다** (0006). 클라이언트가 만든 uuid를 그대로 기본키로 쓰고, 페이로드에 없는 행만 지웁니다. 그래서 `lib/id.ts`의 `createId()`는 **어떤 환경에서도 유효한 uuid를 반환해야 합니다** — 형식이 깨지면 저장이 통째로 실패합니다. PostGIS ↔ `{lat,lng}` 변환은 RPC 안에서 끝나므로 클라이언트는 PostGIS를 모릅니다.
- **업서트에는 `where <table>.map_id = v_id` 가드가 붙어 있습니다.** 없으면 남의 지도 행 id를 일부러 보내 그 행을 자기 지도로 끌어올 수 있습니다. 이 가드 때문에 **테스트에서 여러 지도가 같은 id를 쓰면 행이 조용히 빠집니다** — 의도된 동작입니다.
- **이동수단은 `segments` 테이블이 아니라 `places.mode_to_next`에 있습니다.** 클라이언트의 유일한 근거가 "places 배열 순서 + 각 핀의 다음 구간 이동수단"이라 두 곳에 나누면 동기화 버그만 생깁니다. `segments` 테이블은 비어 있고, 설계안의 `style='manual'`(사용자가 그린 선으로 연결선 대체)과 실제 경로 좌표를 저장할 때 쓸 자리로 남겨 뒀습니다.
- **공유 텍스트 파싱은 단축 URL을 절대 풀지 않습니다.** `naver.me`/`kko.kr`을 서버에서 리졸브하는 건 타사 약관 회색지대입니다. 대신 URL을 버리고 주소 줄마다 최대 10곳으로 나눕니다. 정확한 주소는 Kakao 주소 검색을 먼저 쓰고 이름/지역 키워드 검색으로 후보를 보완합니다 (`lib/kakao/parseShareText.ts` + `app/api/parse-share/route.ts`).
- **`extractDistrict`는 시/도 접두를 먼저 걷어냅니다.** 안 그러면 "서울특별시"의 '시'를 구·군으로 오인합니다.
- **`next lint`는 안 씁니다.** Next 16에서 제거 예정이고 대화형 프롬프트를 띄워서 `eslint.config.mjs` + `eslint .`로 바꿨습니다.
- **dev 서버가 떠 있을 때 `npm run build`를 돌리면 안 됩니다.** 같은 `.next`를 공유해서 vendor-chunk가 깨집니다.

## 앱 작업에서 배운 것 (웹에도 해당하는 것 위주)

세부는 [ios/README.md](ios/README.md)의 "밟은 함정"에 있습니다. 여기에는 **웹 쪽에도
영향이 있거나, 작업 방식 자체에 대한 것**만 옮깁니다.

- **맥이 없습니다. GitHub Actions macOS 러너가 유일한 컴파일러이고 한 번에 약 10분입니다.**
  추측으로 고쳐 밀어 넣지 마세요. SDK 시그니처는 바이너리에서 직접 읽을 수 있습니다
  (`ios/README.md`의 "SDK 시그니처를 확인하는 법").
- **TestFlight 임시 러너의 개발 인증서를 계정에 남기면 안 됩니다.** API 키와
  `-allowProvisioningUpdates`만 준 러너는 매 실행 `Created via API` 개발 인증서를 새로 만들고,
  Apple의 활성 인증서 한도를 채웁니다. 2026-08-05에 쌓인 10개를 정리했습니다.
  `testflight.yml`은 이제 실행을 한 번에 하나로 제한하고, 시작 전 과거 CI 개발 인증서를 정리하며,
  종료 시 이번 인증서도 회수합니다. `scripts/apple_certificate_cleanup.py`는
  `name`/`displayName`에 `Created via API`가 있고 종류가 개발 인증서일 때만 삭제합니다.
  개인 인증서와 배포 인증서는 테스트로 제외합니다.
- **눈으로 볼 방법은 UI 테스트 스크린샷뿐입니다.** 새 화면·새 그림을 만들면 그 흐름을
  걸어가는 UI 테스트를 함께 넣으세요. 안 넣으면 그게 어떻게 보이는지 아무도 모릅니다.
- **그림만으로 원인을 못 좁히면 숫자를 화면 밖으로 내보내세요.** "선이 핀에 안 닿는다"를
  두 번 잘못 고친 뒤에야 `view.accessibilityValue`에 그린 개수를 걸어 원인을 확정했습니다.
  디버거를 붙일 수 없는 환경에서는 이게 디버거 대신입니다.
- **웹이 일부러 그렇게 한 것을 버그로 오해하지 마세요.** 위 사례에서 남은 간격은 사실
  정상이었습니다 — 웹도 `ARROW_TRIM_PX = PIN_RADIUS + 6`으로 핀 앞에서 선을 끊습니다.
  앱에서 이상해 보이면 웹의 같은 부분을 먼저 읽어 보세요.
- **PowerShell로 API를 시험하면 한글이 깨집니다.** `Invoke-RestMethod -Body <문자열>`이
  본문을 ANSI로 보냅니다. curl에 UTF-8 파일을 `--data-binary`로 넘기세요. 운영 DB에
  제목 깨진 지도가 남은 원인이 이것입니다.
- **길찾기 응답의 끝점은 요청 좌표가 아닙니다.** 가장 가까운 도로 노드로 스냅되고 실측
  25m까지 벌어집니다. 웹의 `ACCESS_MIN_PX`, 앱의 `connectorMinimumDeg`가 이걸 잇는
  장치입니다. 임계값을 올리면 정확히 필요한 구간이 잘려 나갑니다.
- **저장 시 서버는 UUID가 아닌 id를 새로 부여합니다.** 그러면 `RoutePath`의 끝점 id가
  어긋나 저장했던 경로가 버려집니다. 클라이언트는 반드시 유효한 uuid를 만들어 보내야
  합니다(웹은 `lib/id.ts`의 `createId()`, 앱은 `UUID().uuidString`).

## 참고

- 설계 원본(§ 번호로 인용되는 문서)은 이 대화의 앞부분에만 있고 프로젝트 파일로는 저장돼 있지 않습니다. 이 저장소는 원래 사용자의 다른 프로젝트(`link-reminder`, 링크 저장 앱)와 융합하지 않고 별도로 새로 만든 것입니다 — 두 프로젝트는 완전히 무관합니다.
- 작업 방식: T1~T15는 설계안 §12.4의 작업 분해 번호를 그대로 따르고 있습니다. 이 문서와 README의 T번호가 그 원문 번호입니다.
